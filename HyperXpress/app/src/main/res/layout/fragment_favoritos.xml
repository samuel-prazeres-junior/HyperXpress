<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginBottom="30dp"
    tools:context=".view.fragment.FavoritosFragment">

    <EditText
        android:id="@+id/edt_pesquisa_favoritos"
        android:layout_width="0dp"
        android:layout_height="60dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <ImageView
        android:id="@+id/iv_pesquisa_favoritos"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/icone_pesquisa"
        app:layout_constraintEnd_toEndOf="@id/edt_pesquisa_favoritos"
        app:layout_constraintTop_toTopOf="@id/edt_pesquisa_favoritos"
        app:layout_constraintBottom_toBottomOf="@id/edt_pesquisa_favoritos" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_favoritos"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_marginBottom="30dp"
        app:layout_constraintTop_toBottomOf="@id/edt_pesquisa_favoritos"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />

</androidx.constraintlayout.widget.ConstraintLayout>